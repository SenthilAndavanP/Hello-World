#include<iostream>
using namespace std;

/*
void Factorial(int Number)
{
if(Number<=1)
  return 1;
  
int N=Number;
  int Init=1;
  while(N>0){
    Init=Init * N--;
  }
  cout<<"Factorial of "<<Number<<"is="<<Init;
}

int main()
{
  N=90;
  Factorial(N);
  return 0;
}
*/
int main()
{
  cout<<"Executing First program in C++ through jenkins";
  return 0;
}
